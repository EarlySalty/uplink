# Nachweise dieser Arbeitsrunde

Stand: 8. September 2026. Das neue Repository enthält eine geprüfte Rust-Grundlage, Offline-Mediennachweise und einen lokal gemessenen Rust-RTMPS-Eingang. Der vollständige Uplink-Neubau ist noch nicht live abgenommen.

`ORCHESTRIERUNG[OR-1]: Klasse hoch | Phase implement | Artefakt: .tasks/2026-09-08-uplink-neubau`

| Gegenstand | Beleg / Ergebnis | Aussagegrenze |
| --- | --- | --- |
| Auftrag und Vertrag | Nutzerauftrag für echte Neuimplementierung; `docs/produktvertrag-v0.3.md` mit 16 Abschnitten, P-01–P-20, F1–F6 und S1–S21 erhalten | Historischer Quellenstand unverändert; VOD-Quelle, -Repository, Speicher und Aufbewahrung bleiben offen |
| Rust-Grundlage | `uplink-core` und `uplink-cli`; geprüfter Fix `385ea02`, anschließende eindeutige Video-Decoderbenennung in `2c4752d` | Deklarierter Szenarioplan, kein ausführbarer Mediengraph und kein OBS-Messwert |
| Lokale Prüfung | Nach Fix 24/24 Tests (17 Core, 7 echte CLI), Formatierung, Clippy und Release-Build bestanden; Code-Agent bestätigt diese Prüfungen samt Beispielszenario erneut auf `2c4752d` | Kein Codec-, Plattform- oder Kapazitätsnachweis |
| Rust-Review | [Native Freigabe auch für `2c4752d`](../../docs/review-rust.md): Drei-Dateien-Diff nachgeprüft, erneut 24 Tests sowie Formatierung, Clippy und Build bestanden; keine offenen kritischen/hohen Befunde | Audio-/Layoutfehler bereits in `385ea02` mit vier zuerst roten Regressionstests behoben; `2c4752d` benennt nur den Video-Decoderzähler eindeutig |
| Sicherheitsreview | [Grundstand `0c9bc4e`](../../docs/review-security.md) ohne aktuellen Blocker; Gitleaks und Cargo Audit bestanden | Fix `385ea02` ändert den Planstatus, keine Sicherheitsgrenze; keine Freigabe für zukünftige öffentliche Pfade |
| Zentraler Gate | [Reguläre Kette](../../docs/review-gate.md) meldete ALLOW für `385ea02`, `2c4752d` und den vollständigen Stand `40ef8fa`; native Rust-Nachprüfung auf `2c4752d` abgeschlossen | Eingebauter Ausfallpfad nach Claude-Kontingentfehler, keine Änderung des Gate; keine offenen Core-Hinweise, vier Archiv-Dock-Punkte vor Aktivierung offen |
| Erhaltener Bestand | [Vier Dock-Quellassets](../../docs/bestandsuebernahme.md) mit [Herkunft und Hashes](../../docs/bestandsmanifest.json); Browser-Syntaxprüfung und bereinigter Secret-Scan bestanden | Inaktive Sicherung; Dashboard und Overlay bleiben im Twitch-Bot; alter Medienkern und alte DB/Secrets nicht importiert |
| Medienversuch | [FFmpeg 8.1.2: AV1 bzw. H.264 plus zwei AAC-Spuren über E-FLV](../../docs/mediennachweis.md), identische komprimierte Spurinhalte und lokale Decodierbarkeit; 6.1.1 lehnt Mehrspurversuch ab | Synthetische Dateien; dokumentierte Zeitverschiebung/Skip-Samples-Verluste; kein OBS-/RTMPS-/Plattformnachweis und keine Engineentscheidung |
| Isolierte Scuffle-Probe | [Stand `32653ca`](../../docs/scuffle-nachweis.md): 13 Debug- und 13 Release-Tests, Formatierung, Clippy, separates Cargo Audit für 43 Crates und Gitleaks grün; finaler Quellstand unabhängig durch nativen Rust-Reviewer geprüft | Ein 35-Byte-AVCC-Header deckte einen Abhängigkeitsfehler auf: Debug-Panik und Release-Bereichsfehler. Der enge Testadapter lässt H.264 nur mit geprüftem Baseline-SequenceHeader zu; keine Produktionsfreigabe. |
| Lokaler Rust-RTMPS-Eingang | [Echte FFmpeg-8-Probe](../../docs/rtmps-nachweis.md): AV1/H.264 jeweils 240 Pakete, drei Header, zwei AAC-Feeds, gleiche Nutzdaten und 0 ms gemeinsame DTS-/PTS-Verschiebung; CA-/SAN-Abweisung vor Publish. 51 Workspace-/Beispieltests; Metadata-Reviewbefunde zuerst rot reproduziert, korrigierter Pfad samt FF8-Probe unabhängig nachgeprüft | Nur Loopback mit injizierter Autorisierung; FFmpeg-Hostnameprüfung nur für gemessene DNS-URL belegt. Öffentlicher Broker, OBS, Rollen und Plattformausgänge offen; Gate/Merge des Bausteins noch ausstehend |
| RTMPS-Sicherheitsreview | [Separater finaler Review](../../docs/review-security-rtmps.md): 59 gezielte Tests, drei Audits und Gitleaks grün; Manifestbereinigung berücksichtigt | Freigabe für den begrenzten lokalen Baustein, keine öffentliche Ingestfreigabe; Gate, Push und Merge bleiben separat |
| Kontrollnachrichten-Nachfix | Gate auf `b6c18f5` lieferte ALLOW mit Hinweis zur falschen EOF-Einstufung. SetChunkSize/WindowAcknowledgementSize benötigen nun genau vier Bytes; falsche Längen erzeugen `InvalidData` und `ProtocolRejected`. Vier neue Regressionen; Vendor jetzt 82 Tests plus Doctest in Debug/Release, Sicherheitsnachreview grün | Zwei geerbte Upstreamtests bleiben ignoriert. Der Gate prüfte nur den Diff ohne Repositorywerkzeuge; finale Gate-/Merge-Prüfung nach dem Nachfix noch offen |
| Repository/Remote | Privates `EarlySalty/uplink`; [PR #1](https://github.com/EarlySalty/uplink/pull/1) und [PR #2](https://github.com/EarlySalty/uplink/pull/2) **MERGED**; geprüfter funktionaler Main-Stand nach PR #2: `001724fdacded263f16681ba0aec8e5f348de601` | Identischer Dateibaum zu `32653ca0a74d3c6a886d0f1a9b700268953153d2` bestätigt; die Branches aus PR #1/#2 wurden lokal und auf GitHub gelöscht. |
| GitHub Actions | [Main-Prüfung 34221066802](https://github.com/EarlySalty/uplink/actions/runs/34221066802): **SUCCESS**; vorherige Main-/Push-/PR-Prüfungen und GitGuardian Secret-Scan ebenfalls erfolgreich | Grundlagen- und isolierter Nachweisbaustein abgeschlossen, keine produktive Medienabnahme |
| Produktivbetrieb | [Vollständige Abnahmen](../../docs/abnahme.md) offen; bestehender Relay-Dienst weiter erforderlich | Keine neue Live-Umschaltung oder Löschung des alten Repositorys behauptet |
| Screenshot | Nur Textbeschreibung des früheren Layout-Screenshots übergeben | Kein Bildasset und keine pixelgenaue Prüfung verfügbar |

## Prüfbefehle und Korrektur

Der unabhängige Rust-Reviewer führte am korrigierten Stand `385ea02` unter anderem `cargo check`, `cargo fmt --check`, `cargo test`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo build --workspace --release --locked` und `git diff --check` erfolgreich aus. Das eigene CLI-Fehlerszenario lieferte nach Fix korrekt Exit 3 statt zuvor Exit 4. Nach der reinen Umbenennung in `2c4752d` bestätigte der Code-Agent erneut 24 Tests, Formatierung, Clippy, Release-Build und das tatsächliche CLI-Beispielszenario. Anschließend prüfte auch der native unabhängige Rust-Reviewer den Drei-Dateien-Diff und gab `2c4752d` nach erneut 24 Tests sowie Formatierung, Clippy und Build frei; die zwischenzeitliche technische Threadgrenze ist erledigt.

Der Sicherheitsreview dokumentiert `gitleaks git /home/nathanael/repos/uplink --redact --no-banner` und `cargo audit --file Cargo.lock`, beide Exit 0. Diese Prüfungen gelten für ihren jeweils genannten Commit; später hinzukommende Eingabe-, Auth- oder Medienpfade benötigen einen eigenen Nachweis.

Ein erster direkter `gate_hook.py --review --base main` gegen `0c9bc4e` endete ohne Urteil mit Exit 2 wegen ausgeschöpftem Claude-Wochenkontingent. Die spätere reguläre Kette nutzte ihren vorhandenen Ausfallpfad und lieferte ALLOW. Das war kein inhaltliches BLOCK und kein Anlass für eine Gate-Änderung.

## Abschluss und anschließender Baustein

Der Grundlagenbaustein wurde über PR #1 als `409ae8db498e342a136556efcb3144a15d2e1846` mit identischem Dateibaum zu `40ef8fa` übernommen. Der geprüfte funktionale Main-Stand nach PR #2 lautet `001724fdacded263f16681ba0aec8e5f348de601`; sein Dateibaum entspricht `32653ca0a74d3c6a886d0f1a9b700268953153d2`. Main-CI ist erfolgreich. Die Branches aus PR #1/#2 wurden lokal und auf GitHub gelöscht. Die Hauptsession koordiniert die weitere Arbeit, zugewiesene Agenten führen sie aus.

Der isolierte Nachweisbaustein ist über PR #2 gemergt. `experiments/scuffle-probe` enthält die portable Probe, kleine synthetische Fixtures und statische FFmpeg-8-Paketmanifeste. Dieser Baustein ließ das Root-Cargo unverändert; die CI enthält isolierte Debug-/Release-Prüfungen. Temporäre Medien sind gelöscht. Beide Bausteine sind abgeschlossen; der folgende RTMPS-Baustein erweitert nun den Workspace, der vollständige Live-Neubau bleibt offen.

Das Gate meldete auch für `32653ca` ALLOW über den regulären Ausfallpfad, ausdrücklich mit „Repository tools unavailable“. Dieser Gate-Lauf ist kein unabhängiger Testnachweis; dafür gelten die separat gemeldeten lokalen Prüfungen und der native Rust-Review.

Die vier UI-Hinweise an den archivierten Docks bleiben vor Aktivierung verbindliche [Prüfpunkte von A-14](../../docs/abnahme.md#archiv-docks-vor-aktivierung-prüfen). Die vollständigen Medien-/Produktabnahmen und offenen Entscheidungen F1–F6 werden durch lokale Freigaben nicht geschlossen. Keine Secrets, Stream-Keys, Plattformtokens oder privaten Medien gehören in diese Akte.
